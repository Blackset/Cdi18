use [Nouvel aeroport]
CREATE type TypeDate from datetime 
CREATE type TypeHoraire from smalldatetime
CREATE type TypeNom from varchar(100) 
CREATE type TypePrenom from varchar(30)
CREATE type TypeAvion from varchar(20)
CREATE type TypeIDConstructeur from smallint
CREATE type TypeIdAeroport from varchar(3)
Create type TypeIdPilote from smallint
create type numVol from varchar(5)
create type TypeVille from varchar(50)
